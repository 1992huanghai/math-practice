# 部署指南 - 将应用托管到 Web Server

这个应用是纯静态网站，可以免费托管到多个平台并获得域名。以下是几种推荐的方案：

## 方案一：GitHub Pages（推荐，免费）

### 优点
- ✅ 完全免费
- ✅ 自动 HTTPS
- ✅ 免费域名：`你的用户名.github.io`
- ✅ 可以绑定自定义域名
- ✅ 自动部署（推送代码即更新）

### 步骤

1. **创建 GitHub 仓库**
   ```bash
   cd /Users/haihuang.hh/Documents/code/math_practice
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **在 GitHub 上创建新仓库**
   - 访问 https://github.com/new
   - 仓库名：`math-practice`（或任意名称）
   - 选择 Public（GitHub Pages 需要公开仓库）
   - 点击 "Create repository"

3. **推送代码到 GitHub**
   ```bash
   git remote add origin https://github.com/你的用户名/math-practice.git
   git branch -M main
   git push -u origin main
   ```

4. **启用 GitHub Pages**
   - 进入仓库页面
   - 点击 Settings → Pages
   - Source 选择：`Deploy from a branch`
   - Branch 选择：`main`，文件夹选择 `/ (root)`
   - 点击 Save

5. **访问你的网站**
   - 几分钟后访问：`https://你的用户名.github.io/math-practice`
   - 或自定义域名（见下方说明）

---

## 方案二：Netlify（推荐，最简单）

### 优点
- ✅ 完全免费
- ✅ 自动 HTTPS
- ✅ 免费域名：`你的项目名.netlify.app`
- ✅ 可以绑定自定义域名
- ✅ 拖拽部署，无需 Git
- ✅ 自动部署（连接 Git 后）

### 步骤

#### 方法 A：拖拽部署（最简单）

1. **访问 Netlify**
   - 访问 https://www.netlify.com
   - 注册/登录账号（可用 GitHub 账号登录）

2. **部署**
   - 点击 "Add new site" → "Deploy manually"
   - 将 `math_practice` 文件夹拖拽到页面
   - 等待部署完成

3. **访问网站**
   - 部署完成后会获得一个免费域名：`随机名称.netlify.app`
   - 可以在 Site settings → Change site name 修改名称

#### 方法 B：通过 Git 部署（推荐）

1. **推送代码到 GitHub**（参考方案一的步骤 1-3）

2. **连接 Netlify**
   - 访问 https://app.netlify.com
   - 点击 "Add new site" → "Import an existing project"
   - 选择 GitHub，授权并选择你的仓库
   - Build command 留空，Publish directory 填写：`.`（当前目录）
   - 点击 "Deploy site"

3. **访问网站**
   - 几分钟后即可访问你的网站

---

## 方案三：Vercel（推荐，速度快）

### 优点
- ✅ 完全免费
- ✅ 自动 HTTPS
- ✅ 免费域名：`你的项目名.vercel.app`
- ✅ 可以绑定自定义域名
- ✅ 全球 CDN，速度快
- ✅ 自动部署

### 步骤

1. **安装 Vercel CLI**
   ```bash
   npm install -g vercel
   ```

2. **部署**
   ```bash
   cd /Users/haihuang.hh/Documents/code/math_practice
   vercel
   ```
   - 按提示登录/注册
   - 选择默认设置即可

3. **访问网站**
   - 部署完成后会显示你的网站地址

4. **或通过网页部署**
   - 访问 https://vercel.com
   - 用 GitHub 账号登录
   - 点击 "Add New Project"
   - 导入你的 GitHub 仓库
   - 直接部署

---

## 方案四：Cloudflare Pages（免费，全球加速）

### 优点
- ✅ 完全免费
- ✅ 自动 HTTPS
- ✅ 免费域名：`你的项目名.pages.dev`
- ✅ 可以绑定自定义域名
- ✅ Cloudflare 全球 CDN

### 步骤

1. **推送代码到 GitHub**（参考方案一的步骤 1-3）

2. **连接 Cloudflare Pages**
   - 访问 https://dash.cloudflare.com
   - 登录/注册账号
   - 选择 "Workers & Pages" → "Create application" → "Pages"
   - 选择 "Connect to Git"
   - 选择 GitHub 并授权
   - 选择你的仓库
   - Build command 留空，Build output directory 填写：`.`（当前目录）
   - 点击 "Save and Deploy"

3. **访问网站**
   - 部署完成后会显示你的网站地址

---

## 绑定自定义域名

所有平台都支持绑定自定义域名：

### 1. 购买域名
- 国内：阿里云、腾讯云、万网等
- 国外：Namecheap、GoDaddy、Cloudflare 等
- 价格：通常每年几十元到几百元不等

### 2. 配置 DNS

#### GitHub Pages
- 在仓库 Settings → Pages → Custom domain 添加域名
- 在域名服务商添加 CNAME 记录指向：`你的用户名.github.io`

#### Netlify
- 在 Site settings → Domain management → Add custom domain
- 按提示添加 DNS 记录（A 记录或 CNAME）

#### Vercel
- 在项目 Settings → Domains → Add Domain
- 按提示配置 DNS

#### Cloudflare Pages
- 在项目 → Custom domains → Set up a custom domain
- 如果域名在 Cloudflare，会自动配置

---

## 推荐方案对比

| 平台 | 免费域名 | 自定义域名 | 部署难度 | 速度 | 推荐度 |
|------|---------|-----------|---------|------|--------|
| GitHub Pages | ✅ | ✅ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| Netlify | ✅ | ✅ | ⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Vercel | ✅ | ✅ | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Cloudflare Pages | ✅ | ✅ | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

**推荐顺序：**
1. **Netlify** - 最简单，拖拽即可部署
2. **Vercel** - 速度快，适合全球访问
3. **GitHub Pages** - 如果已有 GitHub 账号，很方便

---

## 快速开始（Netlify 拖拽部署）

1. 访问 https://www.netlify.com
2. 注册/登录
3. 点击 "Add new site" → "Deploy manually"
4. 将整个 `math_practice` 文件夹拖进去
5. 完成！获得免费域名

**就是这么简单！** 🎉

---

## 注意事项

- 所有平台都提供免费 HTTPS 证书
- 免费套餐通常足够个人项目使用
- 如果流量很大，可能需要升级到付费套餐
- 建议使用 GitHub 托管代码，然后连接这些平台自动部署

